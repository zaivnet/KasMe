<div class="grid gap-4 sm:grid-cols-2"><div><label for="account_id" class="text-sm font-medium">Akun</label><select id="account_id" name="account_id" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected((string) old('account_id', $transaction->account_id ?? '') === (string) $account->id)>{{ $account->name }} ({{ $account->currency }})</option>@endforeach</select><x-form-error name="account_id" /></div><div><label for="type" class="text-sm font-medium">Pergerakan</label><select id="type" name="type" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950">@foreach(App\Models\SavingGoalTransaction::TYPES as $value => $label)<option value="{{ $value }}" @selected(old('type', $transaction->type ?? 'contribution') === $value)>{{ $label }}</option>@endforeach</select><x-form-error name="type" /></div><div><label for="amount" class="text-sm font-medium">Jumlah</label><input id="amount" name="amount" type="number" min="0.01" step="0.01" value="{{ old('amount', $transaction->amount ?? '') }}" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><x-form-error name="amount" /></div><div><label for="transaction_date" class="text-sm font-medium">Tanggal</label><input id="transaction_date" name="transaction_date" type="date" value="{{ old('transaction_date', isset($transaction) ? $transaction->transaction_date->format('Y-m-d') : now(config('app.timezone'))->format('Y-m-d')) }}" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><x-form-error name="transaction_date" /></div><div class="sm:col-span-2"><label for="notes" class="text-sm font-medium">Catatan <span class="text-slate-400">(opsional)</span></label><textarea id="notes" name="notes" rows="3" class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950">{{ old('notes', $transaction->notes ?? '') }}</textarea><x-form-error name="notes" /></div></div>
