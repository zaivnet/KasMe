<div class="grid gap-5 sm:grid-cols-2">
    <div class="sm:col-span-2"><label for="name" class="text-sm font-medium">Nama kategori</label><input id="name" name="name" value="{{ old('name', $category->name ?? '') }}" required maxlength="100" class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><x-form-error name="name" /></div>
    <div><label for="type" class="text-sm font-medium">Jenis</label><select id="type" name="type" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950">@foreach($types as $value => $label)<option value="{{ $value }}" @selected(old('type', $category->type ?? '') === $value)>{{ $label }}</option>@endforeach</select><x-form-error name="type" /></div>
    <div><label for="parent_id" class="text-sm font-medium">Induk <span class="text-slate-400">(opsional)</span></label><select id="parent_id" name="parent_id" class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><option value="">Tanpa induk</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected((string) old('parent_id', $category->parent_id ?? '') === (string) $parent->id)>{{ $types[$parent->type] }} — {{ $parent->name }}</option>@endforeach</select><x-form-error name="parent_id" /></div>
    <x-icon-picker name="icon" :value="$category->icon ?? ''" label="Ikon kategori" />
    <x-color-picker name="color" :value="$category->color ?? '#047857'" label="Warna kategori" />
    <label class="flex items-center gap-3 text-sm sm:col-span-2"><input type="hidden" name="is_active" value="0"><input name="is_active" type="checkbox" value="1" @checked((bool) old('is_active', $category->is_active ?? true)) class="rounded border-slate-300"> Kategori aktif</label>
</div>
