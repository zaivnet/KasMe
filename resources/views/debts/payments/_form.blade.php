<div class="grid gap-4 sm:grid-cols-2">
    <div><label for="account_id" class="text-sm font-medium">Akun</label><select id="account_id" name="account_id" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><option value="">Pilih akun</option>@foreach($accounts as $account)<option value="{{ $account->id }}" @selected((string) old('account_id', $payment->account_id ?? '') === (string) $account->id)>{{ $account->name }} ({{ $account->currency }})</option>@endforeach</select><x-form-error name="account_id" /></div>
    <div><label for="amount" class="text-sm font-medium">Jumlah pembayaran</label><input id="amount" name="amount" type="number" min="0.01" step="0.01" value="{{ old('amount', $payment->amount ?? '') }}" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 tabular-nums dark:border-slate-700 dark:bg-slate-950"><x-form-error name="amount" /></div>
    <div><label for="payment_date" class="text-sm font-medium">Tanggal pembayaran</label><input id="payment_date" name="payment_date" type="date" value="{{ old('payment_date', isset($payment) ? $payment->payment_date->format('Y-m-d') : now(config('app.timezone'))->format('Y-m-d')) }}" required class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950"><x-form-error name="payment_date" /></div>
    <div class="sm:col-span-2"><label for="notes" class="text-sm font-medium">Catatan <span class="text-slate-400">(opsional)</span></label><textarea id="notes" name="notes" rows="3" class="mt-1 block w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 dark:border-slate-700 dark:bg-slate-950">{{ old('notes', $payment->notes ?? '') }}</textarea><x-form-error name="notes" /></div>
</div>
